PySide6==6.11.2
FastAPI==0.111.0
uvicorn==0.30.1
SQLAlchemy==2.0.30

alembic>=1.13,<2

# MQTT integration (Task: "integracja MQTT") - the one, explicitly
# approved new dependency for that task. Eclipse Foundation project,
# dual-licensed EPL-2.0/EDL-1.0 (BSD-style) - no copyleft obligation for
# this proprietary codebase either way. Pure Python, zero transitive
# dependencies (uses only the stdlib socket/ssl), actively maintained
# (2.1.0 released 2024, the de facto standard MQTT client for Python).
# Imported defensively in mqtt_manager.py - its absence does not prevent
# EPW OS from starting (see that module's own docstring).
paho-mqtt==2.1.0
