"""Documentation generators live here as importable modules.

Only so the generators can be imported by their tests - the Markdown and
the workbook beside them are data, not code. Kept empty otherwise.
"""
